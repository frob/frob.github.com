# Copy to .env and fill in. Used by `task deploy:site` via docker --env-file.
# .env is gitignored — never commit real credentials.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
