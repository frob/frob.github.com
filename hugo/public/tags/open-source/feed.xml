<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Open Source on Frank Robert Anderson</title><link>https://www.frobiovox.com/tags/open-source/</link><description>Recent content in Open Source on Frank Robert Anderson</description><generator>Hugo</generator><language>en-us</language><atom:link href="https://www.frobiovox.com/tags/open-source/feed.xml" rel="self" type="application/rss+xml"/><item><title>Introducing webform relay; forms without a backend</title><link>https://www.frobiovox.com/posts/2026/09/08/introducing-webform-relay-forms-without-a-backend/</link><pubDate>Tue, 08 Sep 2026 00:00:00 +0000</pubDate><guid>https://www.frobiovox.com/posts/2026/09/08/introducing-webform-relay-forms-without-a-backend/</guid><description><![CDATA[<p>A static site is a wonderful thing right up until somebody asks for a contact form.</p>
<p>Everything else about the static site story holds together. Content in markdown, build in CI, files on a CDN, no server to patch, no database to back up, no PHP version to worry about at 2am. Then one form shows up and suddenly the answer is &ldquo;well, we&rsquo;ll need a backend.&rdquo; Not a big backend. A backend that accepts a POST, validates four fields, and sends an email. And now you own a server, or a container, or a runtime version, or a third-party SaaS that will start charging per submission the moment anyone actually uses the thing.</p>
<p>I have watched this play out enough times. The form is never the interesting part of the project and it is always the part that drags a whole hosting story behind it. In other words I have written this form enough times and enough ways that I don&rsquo;t want to have to write it again. I know everything that it needs to do, in every variation. I kind of miss <a href="https://www.drupal.org/project/webform">Drupal&rsquo;s webform module</a> but I cannot use that on a static site.</p>
<p>So I wrote <a href="https://gitlab.com/frob/webform-relay">webform relay</a>. Point a <code>&lt;form&gt;</code> at a URL, and it validates the submission and relays it to wherever it needs to go &ndash; email through SES, a plain SMTP server, a generic webhook, or Salesforce Web2Lead. There is no application code to write. There is no server. There is one YAML file. I even threw in some form conditionals and static content printing through htmx.</p>
<h2 id="the-whole-configuration">The whole configuration</h2>
<p>This is a working config. Not a fragment, not pseudo-config &ndash; this is the file:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span><span style="color:#75715e"># webform.yaml</span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">cache_ttl</span>: <span style="color:#ae81ff">30s</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#f92672">forms</span>:
</span></span><span style="display:flex;"><span>  <span style="color:#f92672">contact</span>:
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">fields</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">name</span>: <span style="color:#ae81ff">name</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">required</span>: <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">name</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">required</span>: <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">type</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">name</span>: <span style="color:#ae81ff">message</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">required</span>: <span style="color:#66d9ef">true</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">type</span>: <span style="color:#ae81ff">textarea</span>
</span></span><span style="display:flex;"><span>    <span style="color:#f92672">outputs</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">type</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">to</span>: <span style="color:#ae81ff">you@example.com</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">subject</span>: <span style="color:#e6db74">&#34;New contact form submission&#34;</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">mapping</span>:
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">from_name</span>: <span style="color:#ae81ff">name</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">from_email</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">body</span>: <span style="color:#ae81ff">message</span>
</span></span></code></pre></div><p><code>fields</code> is what the form accepts and what gets validated. <code>outputs</code> is where it goes. <code>mapping</code> renames form fields into whatever the destination calls them, and an output sends <em>only</em> what the mapping lists &ndash; nothing gets passed through implicitly.</p>
<p>That file lives in S3, not in the deployment. Change it, upload it, and the running function picks it up within <code>cache_ttl</code>. Adding a field or a second recipient is a file upload, not a redeploy. That decision drove more of the design than anything else, because the point of the whole exercise is to not think about the form again after you build it.</p>
<h2 id="what-it-does">What it does</h2>
<p>The config file above is the minimum. The rest of the surface, since the list of things it won&rsquo;t do further down is otherwise going to read as the longer list:</p>
<ul>
<li><strong>Four destination types.</strong> <code>email</code> (SES), <code>smtp</code> (any server &ndash; TLS mode picked from the port, 465 implicit, 587 STARTTLS, minimum TLS 1.2, PLAIN auth when a username is set), <code>http_post</code> (a generic url-encoded webhook), and <code>salesforce_web2lead</code>.</li>
<li><strong>Fan-out.</strong> Outputs are a list and all of them run on every submission. Per-output <code>on_error: fail | continue</code> decides whether a dead downstream takes the whole request down with it or just gets logged.</li>
<li><strong>Field mapping and static values.</strong> <code>mapping</code> renames form fields into whatever the destination calls them; <code>static</code> injects fixed values like a Salesforce org id or a <code>lead_source</code>. An output sends only what those two list.</li>
<li><strong>A real field model.</strong> <code>text</code>, <code>textarea</code>, <code>email</code>, <code>url</code>, <code>tel</code>, <code>number</code>, <code>date</code>, <code>select</code>, <code>radios</code>, <code>checkboxes</code>, <code>checkbox</code>, <code>hidden</code>, plus a non-input <code>html</code> type for static content between fields. Labels, help text, hints, placeholders, defaults, <code>autocomplete</code>, arbitrary extra attributes, and <code>prefix</code>/<code>suffix</code>/<code>wrapper</code> for when your CSS framework wants markup around the input.</li>
<li><strong>Server-side validation.</strong> <code>min_length</code>, <code>max_length</code>, <code>min</code>, <code>max</code>, <code>step</code>, <code>pattern</code> (RE2), <code>one_of</code>, <code>min_selected</code>, <code>max_selected</code>, <code>matches_field</code>, <code>differs_from</code> &ndash; with a per-constraint <code>messages</code> block when the default wording is wrong for your form.</li>
<li><strong>Multi-value fields that survive the trip.</strong> Checkbox groups and multi-selects stay multi-valued through validation, mapping, and every output type. Webhooks get the repeated key, email gets one line per value.</li>
<li><strong>Conditional fields.</strong> <code>show_when</code> shows or hides any field based on another field&rsquo;s value (<code>equals</code>, <code>one_of</code>, <code>filled</code>, <code>empty</code>, and friends, combinable with <code>all</code>/<code>any</code>), evaluated server-side and re-rendered live over htmx. Hidden fields collapse to a value-preserving <code>&lt;input type=&quot;hidden&quot;&gt;</code> and skip validation.</li>
<li><strong>Strict parsing.</strong> Unknown or misspelled config keys are a hard error at load, not a silently ignored line. Typing <code>requried</code> fails immediately instead of at 4pm on a Friday.</li>
<li><strong>Templating in output values.</strong> <code>{{ .fields.email }}</code> in a subject line, <code>{{ env &quot;SMTP_PASSWORD&quot; }}</code> for a secret out of the Lambda environment. Locked to a small function whitelist, and the syntax is checked when the config loads rather than when a submission arrives. URLs are never templated.</li>
<li><strong>Accessible generated markup.</strong> <code>task generate:form FORM=contact</code> or <code>GET /api/v1/form/{form}</code> renders the field model to HTML with every control labelled, <code>fieldset</code>/<code>legend</code> around option groups, and <code>aria-describedby</code> wiring. The form you serve and the form the server validates come from one definition.</li>
<li><strong>htmx-native responses.</strong> <code>HX-Request</code> gets HTML fragments &ndash; confirmation on success, or the form re-rendered with inline errors, values intact, <code>aria-invalid</code> set. Everything else gets the same JSON it always got.</li>
<li><strong>Three spam layers, all optional.</strong> Honeypot field, the signed min-fill token, and server-side captcha verification for reCAPTCHA v2/v3, hCaptcha, or Turnstile.</li>
<li><strong>CORS and throttling as deploy parameters.</strong> <code>ALLOWED_ORIGINS</code> for browser reads, and stage throttling that defaults to 10 requests a second with a burst of 20 so a public endpoint can&rsquo;t run up a bill.</li>
<li><strong>A CLI and a JSON Schema.</strong> <code>webform-relay config validate</code>, <code>form list</code>, <code>form html</code>, and <code>form schema</code> &ndash; which prints the schema so your editor can autocomplete the config file and yell at you before S3 ever sees it.</li>
</ul>
<h2 id="getting-it-running">Getting it running</h2>
<p>It runs as a Go binary in a container on Lambda, behind API Gateway, deployed with SAM. Everything &ndash; Go, the AWS CLI, SAM itself &ndash; runs in Docker through the Taskfile, so the only things you install on the host are Docker and Task.</p>
<p>It is meant to be vendored into your own project rather than forked, using <a href="/posts/2026/08/30/introducing-v-the-easiest-vendor-utility/">v</a>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>v add https://gitlab.com/frob/webform-relay &lt;tag&gt;
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>task relay:tools:build       <span style="color:#75715e"># build the SAM/AWS CLI tools image (once)</span>
</span></span><span style="display:flex;"><span>task relay:aws:bootstrap     <span style="color:#75715e"># one-time: deploy IAM user + both S3 buckets</span>
</span></span><span style="display:flex;"><span>task relay:build:container
</span></span><span style="display:flex;"><span>task relay:deploy:app
</span></span></code></pre></div><p>Then the config:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>task relay:validate:config CONFIG<span style="color:#f92672">=</span>webform.yaml
</span></span><span style="display:flex;"><span>task relay:upload:config CONFIG<span style="color:#f92672">=</span>webform.yaml
</span></span></code></pre></div><p>And the form. You can write it yourself:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&lt;<span style="color:#f92672">form</span> <span style="color:#a6e22e">method</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;post&#34;</span> <span style="color:#a6e22e">action</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://&lt;api-id&gt;.execute-api.&lt;region&gt;.amazonaws.com/api/v1/submit/contact&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;<span style="color:#f92672">input</span> <span style="color:#a6e22e">name</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;name&#34;</span> <span style="color:#a6e22e">type</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;text&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;<span style="color:#f92672">input</span> <span style="color:#a6e22e">name</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;email&#34;</span> <span style="color:#a6e22e">type</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;email&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;<span style="color:#f92672">textarea</span> <span style="color:#a6e22e">name</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;message&#34;</span>&gt;&lt;/<span style="color:#f92672">textarea</span>&gt;
</span></span><span style="display:flex;"><span>  &lt;<span style="color:#f92672">button</span> <span style="color:#a6e22e">type</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;submit&#34;</span>&gt;Send&lt;/<span style="color:#f92672">button</span>&gt;
</span></span><span style="display:flex;"><span>&lt;/<span style="color:#f92672">form</span>&gt;
</span></span></code></pre></div><p>Or let it write the form for you from the same field definitions it validates against, so the two can&rsquo;t drift apart:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>task generate:form FORM<span style="color:#f92672">=</span>contact
</span></span></code></pre></div><p>The generated markup is the accessible version &ndash; every control labelled, <code>fieldset</code> and <code>legend</code> around option groups, <code>aria-describedby</code> wired to help text.</p>
<p>Test it:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl -X POST <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  https://&lt;api-id&gt;.execute-api.&lt;region&gt;.amazonaws.com/api/v1/submit/contact <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  -H <span style="color:#e6db74">&#34;Content-Type: application/x-www-form-urlencoded&#34;</span> <span style="color:#ae81ff">\
</span></span></span><span style="display:flex;"><span>  -d <span style="color:#e6db74">&#34;name=Jane&amp;email=jane@example.com&amp;message=Hello&#34;</span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># → {&#34;message&#34;:&#34;ok&#34;}</span>
</span></span></code></pre></div><p>That&rsquo;s the loop. One YAML file, one deploy, no code.</p>
<h2 id="more-than-one-destination">More than one destination</h2>
<p>Outputs are a list, and every output on a form runs on every submission. The case that actually motivated this was a lead form that has to reach both a CRM and a human inbox:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-yaml" data-lang="yaml"><span style="display:flex;"><span>    <span style="color:#f92672">outputs</span>:
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">type</span>: <span style="color:#ae81ff">salesforce_web2lead</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">url</span>: <span style="color:#ae81ff">https://webto.salesforce.com/servlet/servlet.WebToLead?encoding=UTF-8</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">mapping</span>:
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">last_name</span>: <span style="color:#ae81ff">name</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">email</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">description</span>: <span style="color:#ae81ff">message</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">static</span>:
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">oid</span>: <span style="color:#e6db74">&#34;00Dxxxxxxxxxxxxxxx&#34;</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">retURL</span>: <span style="color:#e6db74">&#34;https://yoursite.com/thank-you&#34;</span>
</span></span><span style="display:flex;"><span>          <span style="color:#f92672">lead_source</span>: <span style="color:#ae81ff">Website</span>
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span>      - <span style="color:#f92672">type</span>: <span style="color:#ae81ff">email</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">to</span>: <span style="color:#ae81ff">sales@yourcompany.com</span>
</span></span><span style="display:flex;"><span>        <span style="color:#f92672">subject</span>: <span style="color:#e6db74">&#34;New lead from website&#34;</span>
</span></span></code></pre></div><p>Web2Lead is a public servlet that takes a url-encoded POST, so there are no Salesforce API credentials involved. It is a <code>http_post</code> with a different name and a note that says what it is for. <code>static</code> injects the fixed values &ndash; your org id, the return URL, the lead source. The bonus here is validation and complex form elements, it&rsquo;s much easier when you control the form and then send it to Salesforce behind the scenes.</p>
<p>Each output takes an <code>on_error</code> of <code>fail</code> or <code>continue</code>, so a flaky webhook can be allowed to fail without taking the email with it.</p>
<h2 id="it-speaks-htmx">It speaks htmx</h2>
<p>A plain form POST does a full page navigation, and for a lot of static sites that&rsquo;s fine &ndash; you get a JSON body back, which is not fine, but that&rsquo;s what a <code>retURL</code> is for.</p>
<p>The better answer, if you&rsquo;re already on a static site, is <a href="https://htmx.org">htmx</a>. The relay is built for it, and the whole integration hangs off one rule: if the request carries <code>HX-Request: true</code>, which htmx sends on everything, the response is an HTML fragment instead of JSON. Every other client keeps getting the same JSON it always got, byte for byte.</p>
<p>The minimum version:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&lt;<span style="color:#f92672">form</span> <span style="color:#a6e22e">hx-post</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://&lt;relay-host&gt;/api/v1/submit/contact&#34;</span>
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">hx-target</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;#result&#34;</span> <span style="color:#a6e22e">hx-target-error</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;#errors&#34;</span>
</span></span><span style="display:flex;"><span>      <span style="color:#a6e22e">hx-ext</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;response-targets&#34;</span>&gt;
</span></span><span style="display:flex;"><span>  <span style="color:#75715e">&lt;!-- fields … --&gt;</span>
</span></span><span style="display:flex;"><span>  &lt;<span style="color:#f92672">div</span> <span style="color:#a6e22e">id</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;errors&#34;</span>&gt;&lt;/<span style="color:#f92672">div</span>&gt;
</span></span><span style="display:flex;"><span>&lt;/<span style="color:#f92672">form</span>&gt;
</span></span><span style="display:flex;"><span>&lt;<span style="color:#f92672">div</span> <span style="color:#a6e22e">id</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;result&#34;</span>&gt;&lt;/<span style="color:#f92672">div</span>&gt;
</span></span></code></pre></div><p>Three outcomes, three fragments:</p>
<ul>
<li>Success returns <code>&lt;div class=&quot;form-confirmation&quot;&gt;</code> with the form&rsquo;s <code>confirmation</code> message, which is a per-form config key and defaults to <code>Thank you.</code></li>
<li>A validation failure returns the <strong>whole form, re-rendered</strong> &ndash; the submitter&rsquo;s values back in the boxes, each failing field&rsquo;s message inline in a <code>&lt;p class=&quot;field-error&quot;&gt;</code> wired up through <code>aria-invalid</code> and <code>aria-describedby</code>, and a fresh anti-spam token. Swap it over the original with <code>hx-swap=&quot;outerHTML&quot;</code> and you have server-side validation that behaves like client-side validation, without writing any addtional Javascript.</li>
<li>Anything else &ndash; an expired token, a dead downstream &ndash; returns <code>&lt;ul class=&quot;form-errors&quot;&gt;</code> with the messages, escaped.</li>
</ul>
<p>The status codes are real, which is the part that bites people: validation is a <code>422</code>, an unknown form is a <code>404</code>. htmx only swaps <code>2xx</code> by default, so error fragments need the <a href="https://htmx.org/extensions/response-targets/"><code>response-targets</code></a> extension and an <code>hx-target-error</code>. That&rsquo;s the <code>hx-ext</code> line above.</p>
<p>You don&rsquo;t have to write the form at all, either. <code>GET /api/v1/form/{form}</code> serves the whole thing as a fragment, so the page can be a hole:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&lt;<span style="color:#f92672">div</span> <span style="color:#a6e22e">hx-get</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://&lt;relay-host&gt;/api/v1/form/contact&#34;</span>
</span></span><span style="display:flex;"><span>     <span style="color:#a6e22e">hx-trigger</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;load&#34;</span> <span style="color:#a6e22e">hx-swap</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;innerHTML&#34;</span>&gt;&lt;/<span style="color:#f92672">div</span>&gt;
</span></span></code></pre></div><p>That endpoint reads current values from the query string, so it repopulates and re-evaluates conditional visibility on every render. Which is what makes <code>show_when</code> work without any JavaScript of my own: when a field controls another field&rsquo;s visibility, the relay emits the re-render attributes onto the controller itself &ndash;</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>hx-get=&#34;…/api/v1/form/contact&#34; hx-trigger=&#34;change&#34;
</span></span><span style="display:flex;"><span>hx-target=&#34;closest form&#34; hx-swap=&#34;outerHTML&#34; hx-include=&#34;closest form&#34;
</span></span></code></pre></div><p>Changing it round-trips the form and gets back a version with the dependent fields shown or hidden. Server-rendered conditional forms, and the only client-side dependency is htmx itself.</p>
<p>If you&rsquo;d rather own your markup and just need the token, <code>GET /api/v1/token/{form}</code> returns nothing but the hidden input:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-html" data-lang="html"><span style="display:flex;"><span>&lt;<span style="color:#f92672">input</span> <span style="color:#a6e22e">hx-get</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;https://&lt;relay-host&gt;/api/v1/token/contact&#34;</span>
</span></span><span style="display:flex;"><span>       <span style="color:#a6e22e">hx-trigger</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;load&#34;</span> <span style="color:#a6e22e">hx-swap</span><span style="color:#f92672">=</span><span style="color:#e6db74">&#34;outerHTML&#34;</span>&gt;
</span></span></code></pre></div><p>One deployment note. htmx sends <code>HX-Request</code> and friends, those are not CORS-simple headers, and a cross-origin request with them triggers a preflight. The <code>AllowHeaders</code> on the API already lists them, so this works out of the box &ndash; but it&rsquo;s the reason that list exists, and if you ever hand-roll the CORS config, that&rsquo;s what you&rsquo;ll have broken.</p>
<h2 id="spam-without-a-database">Spam, without a database</h2>
<p>The obvious problem with a public endpoint is that it is a public endpoint. There are three layers, all optional.</p>
<p>The honeypot is the boring one: name a decoy field, and any submission that fills it gets dropped and receives the exact same <code>200 {&quot;message&quot;:&quot;ok&quot;}</code> a real submission gets. There is no signal to learn from.</p>
<p>The one I like better is the min-fill token. When the form is rendered, the server mints an HMAC-signed timestamp bound to that form id and drops it in a hidden input. On submit, it checks how long the human took. Under the threshold, the submission is a bot, and it gets the same silent success. A genuinely stale page gets a distinct &ldquo;form expired&rdquo; response instead, because that one is a real person who left a tab open.</p>
<p>The part I care about is that this needs no storage. The signature is over <code>form_id</code> and <code>issued_at</code>, so verifying elapsed time takes no session, no table, no Redis. The function stays stateless, which is the entire reason it costs nothing when nobody is submitting.</p>
<p>It is also not what a captcha is, and the docs say so: the token isn&rsquo;t single-use, so with no server state it can be replayed until it expires. It raises the cost of automation. It is not proof of a human. For that there is a <code>captcha</code> block that verifies reCAPTCHA v2/v3, hCaptcha, or Turnstile server-side before anything gets relayed.</p>
<h2 id="two-common-gotchas-worth-pointing-out">Two common gotchas worth pointing out</h2>
<p>Both of these were surfaced the first time somebody ran the deploy path end to end on a fresh account.</p>
<p>The deploy user&rsquo;s IAM policy was applied as an inline user policy. Inline user policies cap at 2048 bytes. The rendered policy is about 3.4KB, which means it had been over the limit for as long as it had existed, and <code>task aws:bootstrap-user</code> failed with <code>LimitExceeded: Maximum policy size of 2048 bytes exceeded</code>. The fix was a customer-managed policy instead &ndash; 6144 bytes, and versioned, which brings its own wrinkle since a managed policy holds five versions and then refuses new ones, so the update path prunes before it writes.</p>
<p>The other one is <code>ALLOWED_ORIGINS</code>. API Gateway wants full origins. Give it a bare hostname, a trailing slash, or a space after a comma and it rejects the value with <code>Invalid format for origin</code> &ndash; but only while CloudFormation is in the middle of creating the HTTP API, several minutes in, leaving a <code>ROLLBACK_COMPLETE</code> stack that has to be torn down before you can try again. There is now a one-line precondition on <code>task deploy:app</code> that checks each entry against <code>^(\*|https?://[^/[:space:]]+)$</code> and fails in about a second. Very little of that work was clever. It just moved a failure from four minutes and a manual teardown to instantly, which is most of what deploy tooling is for.</p>
<p>A related note on CORS, since it confuses people: <code>ALLOWED_ORIGINS</code> controls who may <em>read the response</em> from a browser. A plain HTML form POST isn&rsquo;t subject to CORS at all and works no matter what you set. It only matters if you&rsquo;re submitting with <code>fetch</code>.</p>
<h2 id="what-it-doesnt-do">What it doesn&rsquo;t do</h2>
<ul>
<li><strong>No file uploads.</strong> Submissions are <code>application/x-www-form-urlencoded</code> only. No multipart, no attachments, and that is a deliberate non-goal rather than a todo.</li>
<li><strong>No durability on failure.</strong> If a required output hard-fails &ndash; SES throttles you, the webhook is down &ndash; the endpoint returns a 500 and the submission is gone. There is no queue and no retry yet. This is the top item for 0.4.x and there is a design doc weighing SQS-and-worker against an S3 spool, but it is not built, and I would rather say that plainly than let someone find out during a product launch.</li>
<li><strong>No per-IP rate limiting.</strong> There is stage-level throttling at 10 requests a second with a burst of 20, which caps your bill. It does not stop one abusive address from eating the whole budget. Per-IP means WAF, WAF is about five dollars a month before requests, and I have not decided that is worth it by default.</li>
<li><strong>Secrets sit in the config file.</strong> An SMTP password or a captcha secret in <code>webform.yaml</code> is plaintext in your S3 bucket. There&rsquo;s a <code>{{ env &quot;NAME&quot; }}</code> template function to pull them from the Lambda environment instead, which is what I&rsquo;d use, but there is no first-class Secrets Manager integration.</li>
<li><strong>No dashboard.</strong> There is nowhere to browse what has been submitted. There&rsquo;s an optional second stack that archives raw submissions to a private bucket through a Lambda-free API Gateway to S3 proxy, and some tasks to read them back, and that is the entire story. The Dashboard is another app. Though it is on the roadmap to add a csv export to the S3 proxy.</li>
</ul>
<h2 id="is-this-for-you">Is this for you?</h2>
<p>If you already run a backend, no. Add a route. You have all the machinery. Drupal has the fantasic <a href="https://www.drupal.org/project/webform">webform</a> and I got a good deal of inspiration there when building this.</p>
<p>If you&rsquo;re on a host that includes forms in the price and you&rsquo;re happy with it, and you don&rsquo;t need complex submission handling, also no.</p>
<p>But if you have a static site, or a Go binary, or a game&rsquo;s landing page, or anything else where the form is the only thing standing between you and having no server at all &ndash; this is the smallest, robust, version of that I could build. One YAML file in a bucket. No idle cost. No runtime to keep patched.</p>
<p>It&rsquo;s at <a href="https://gitlab.com/frob/webform-relay">gitlab.com/frob/webform-relay</a>, AGPLv3 licensed, with the docs published from the same repo. It is not on GitHub, which I mention because I recently had to change all the docs pages where the GenAI decieded to link to github &ndash;alas, that is what writing software looks like in 2026.</p>
]]></description></item><item><title>Introducing v; the easiest vendor utility</title><link>https://www.frobiovox.com/posts/2026/08/30/introducing-v-the-easiest-vendor-utility/</link><pubDate>Sun, 30 Aug 2026 16:54:03 -0700</pubDate><guid>https://www.frobiovox.com/posts/2026/08/30/introducing-v-the-easiest-vendor-utility/</guid><description><![CDATA[<p>Every so often you need a library in your project and there is no package manager standing by to hand it to you. Maybe it is a repository you own that was never published anywhere. Maybe the upstream project ships no releases. Maybe the language ecosystem in question has no dependency story worth the name. So you do what people have always done: you copy the source into your project and get on with your day.</p>
<p>That works fine right up until the moment you need to answer a question about it. Which commit did I copy? Has upstream changed since? Did somebody edit these files after they landed here? A <code>vendor/</code> directory that got there by hand is an undated snapshot of unknown provenance. Six months later it is indistinguishable from code you wrote yourself. I have been handed projects like that and I have been the person to fix it.</p>
<p>I got tired of that, so I wrote <a href="https://github.com/frob/v">v</a>.</p>
<h2 id="what-it-does">What it does</h2>
<p>Three things, and deliberately nothing else:</p>
<ol>
<li>It resolves whatever ref you give it &ndash;a branch, a tag, a commit hash &ndash;to an exact commit hash.</li>
<li>It downloads the repository&rsquo;s contents into <code>vendor/</code>, without the <code>.git</code> directory.</li>
<li>It writes down what it did in a <code>vendors.toml</code> file in your project root.</li>
</ol>
<p>That&rsquo;s the whole tool. The interesting part is the third one.</p>
<h2 id="a-real-world-use-case">A real world use case</h2>
<p>This blog <a href="/posts/2026/04/23/add-a-json-api-to-any-static-site-generator/">runs on Hugo</a> with a theme I maintain in a GitLab repository. The theme is not on any registry. It&rsquo;s not a Go module. It is a pile of layouts and CSS that belongs to me and lives somewhere else.</p>
<p>The usual options for that situation are a git submodule or a Hugo module. Both work. Both also mean that anyone who clones this site now has to know about a second dependency system, and that my build has an opinion about network access. I didn&rsquo;t want a dependency system. I wanted the files to be in the repository, the way files are, plus a note saying where they came from.</p>
<p>Here is what that note looks like on this site:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-toml" data-lang="toml"><span style="display:flex;"><span>[<span style="color:#e6db74">&#39;git@gitlab.com:frob/frobiovox-theme.git&#39;</span>]
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">url</span> = <span style="color:#e6db74">&#39;git@gitlab.com:frob/frobiovox-theme.git&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">ref</span> = <span style="color:#e6db74">&#39;0.0.x&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">commit</span> = <span style="color:#e6db74">&#39;d0b29bbda2560331ddb849b0dcefc65b709454f4&#39;</span>
</span></span><span style="display:flex;"><span><span style="color:#a6e22e">path</span> = <span style="color:#e6db74">&#39;hugo/themes/frobiovox-theme&#39;</span>
</span></span></code></pre></div><p>Four fields. Where it came from, what I asked for, what that actually resolved to, and where it landed. The <code>commit</code> field is the one doing the real work. A branch name drifts &ndash;<code>0.0.x</code> means something different today than it did in June. The hash doesn&rsquo;t. It pins exactly what is sitting in <code>hugo/themes/frobiovox-theme</code> right now.</p>
<p>Commit that file to your repository and the provenance survives. Someone else can see what you vendored, when it moved, and by how much, just by reading the git history of a four-line TOML entry.</p>
<h2 id="using-it">Using it</h2>
<p>Install it with Homebrew:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>brew install frob/v/v
</span></span></code></pre></div><p>Or with the shell script, which detects your OS and package manager and falls back to dropping a binary in <code>/usr/local/bin</code>:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>curl -sSf https://raw.githubusercontent.com/frob/v/0.2.x/install.sh | sh
</span></span></code></pre></div><p>There are <code>.deb</code>, <code>.rpm</code>, and Arch packages on the releases page too.</p>
<p>Vendoring something is one command:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>v add https://github.com/example/repo
</span></span></code></pre></div><p>That resolves the remote&rsquo;s default branch, downloads the contents to <code>vendor/github.com/example/repo</code>, and records the entry. Pin to a tag instead:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>v add https://github.com/example/repo v1.2.3
</span></span></code></pre></div><p>Or put it somewhere that is not <code>vendor/</code>, which is what I do for the theme:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>v add -d hugo/themes/frobiovox-theme git@gitlab.com:frob/frobiovox-theme.git 0.0.x
</span></span></code></pre></div><p>Later, when upstream has moved:</p>
<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span><span style="color:#75715e"># everything, to the latest commit on each recorded ref</span>
</span></span><span style="display:flex;"><span>v update
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># just the one</span>
</span></span><span style="display:flex;"><span>v update https://github.com/example/repo
</span></span><span style="display:flex;"><span>
</span></span><span style="display:flex;"><span><span style="color:#75715e"># and switch it to a different tag while you are at it</span>
</span></span><span style="display:flex;"><span>v update https://github.com/example/repo v2.0.0
</span></span></code></pre></div><p><code>update</code> tells you what changed:</p>
<pre tabindex="0"><code>updated https://github.com/example/repo a1b2c3d -&gt; e4f5a6b
</code></pre><p>Then <code>git diff</code> shows you exactly what came along with it. That is the part I actually wanted. Not automation &ndash;visibility. When a vendored dependency changes under me, I want it to show up in a code review like everything else.</p>
<h2 id="why-no-git-directory">Why no <code>.git</code> directory</h2>
<p>Because a vendored tree should be files, not a repository. Submodules are a repository inside your repository, and they bring the whole apparatus with them: a second checkout state, detached heads, the <code>--recursive</code> flag everyone forgets, clones that come down empty. Subtrees avoid some of that and replace it with a merge strategy you have to think about.</p>
<p><code>v</code> sidesteps both by not being a git integration at all. It downloads the contents and deletes the history. Your repository stays one repository. <code>git clone</code> gets you everything. There is no second state to be out of sync with, because there is no second state. The metadata a <code>.git</code> directory would have carried lives in <code>vendors.toml</code> instead, in four lines you can read.</p>
<p>The tradeoff is real and I will name it: you can&rsquo;t <code>git log</code> a vendored dependency in place, and if you edit those files, <code>v update</code> will happily overwrite your edits. Vendored code is read-only code. If you want to change it, change it upstream.</p>
<h2 id="what-it-does-not-do">What it does not do</h2>
<p>It is deliberately small. Some things are missing on purpose:</p>
<ul>
<li>No integrity check. Nothing currently verifies that a vendored tree still matches its recorded commit. If someone edits a file in <code>vendor/</code>, <code>vendors.toml</code> will keep insisting everything is fine. This is the next thing I am building.</li>
<li>No round trip. If you want to fix a bug in a vendored dependency and send it upstream, you are on your own: clone it separately, do the work there, then re-vendor. I would like to make that a single command.</li>
<li>No selective vendoring. Every entry in <code>vendors.toml</code> gets its files checked into your tree. There is no way yet to track a dependency&rsquo;s provenance while keeping its source out of your repository, which you might want for license reasons.</li>
<li>No dependency resolution, ever. If the thing you vendored has its own dependencies, that&rsquo;s your problem. <code>v</code> copies one repository at one commit. It&rsquo;s not going to grow a solver, and this is a feature.</li>
</ul>
<h2 id="is-this-for-you">Is this for you?</h2>
<p>Probably not, most of the time. If your language has a package manager and your dependency is published to it, use the package manager. That is what it is for.</p>
<p>But when you are outside that path &ndash;a private repo, an unpublished theme, a library whose maintainer stopped cutting releases in 2019, a language where &ldquo;just copy it in&rdquo; is the community norm &ndash;you&rsquo;re going to copy the source anyway. All <code>v</code> does is make sure that when you do, you wrote down where it came from.</p>
<p>The code is at <a href="https://github.com/frob/v">github.com/frob/v</a>. It&rsquo;s MIT licensed, it&rsquo;s a single Go binary with no runtime dependencies, and it is small enough that you can read the whole thing in an afternoon.</p>
]]></description></item></channel></rss>