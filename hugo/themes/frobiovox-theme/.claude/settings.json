{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'REMINDER: The user edits files directly between prompts. Always read the current file before editing or rewriting it — never assume it matches what was last seen.'"
          }
        ]
      }
    ]
  }
}
